# Cloudflare Workers configuration (Workers-only, not Pages)
name = "pettypet"
main = "src/worker.ts"
compatibility_date = "2026-04-16"
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm run build"
watch_paths = ["src/**/*.ts"]
