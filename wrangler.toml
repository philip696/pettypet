# Cloudflare Workers configuration
# GitHub Actions uses wrangler-action to deploy this
# NOT for Cloudflare Pages auto-deployment

name = "pettypet"
main = "src/worker.ts"
compatibility_date = "2026-04-16"
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm run build"
