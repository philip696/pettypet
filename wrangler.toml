# Cloudflare Pages configuration for frontend
# Server is deployed separately to Workers

name = "pettypet-pages"
compatibility_date = "2026-04-16"
pages_build_output_dir = "out"

[build]
command = "npm run build"
destination_dir = "out"

[build.environment]
NEXT_PUBLIC_API_URL = "https://pettypet.philip-dewanto.workers.dev"
